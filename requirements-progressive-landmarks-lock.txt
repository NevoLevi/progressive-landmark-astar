# Exact direct-package versions used by the active landmark artifact.
# Search, replay, and protocol verification otherwise use the standard library.
matplotlib==3.10.3
pytest==8.3.5
setuptools==80.8.0
